# Rename this file to ".env" (same folder) before running.
#
# ===== Source mailbox (school) =====
SRC_EMAIL=
SRC_PASSWORD=
IMAP_HOST=mail.gzus.edu.cn
IMAP_PORT=993
IMAP_SSL=true
IMAP_FOLDER=INBOX

# ===== Forward via SMTP (can be same as source, or another account) =====
# If you want to forward as the school mailbox, put its SMTP + password here.
# If you want to forward as a QQ mailbox, use QQ SMTP authorization code as password.
SMTP_USER=
SMTP_PASSWORD=
SMTP_HOST=mail.gzus.edu.cn
SMTP_PORT=465
SMTP_SSL=true

# ===== Destination =====
DEST_EMAIL=

# ===== Schedule =====
POLL_INTERVAL_SECONDS=3600

